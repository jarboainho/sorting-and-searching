PROCEDURE insertion_sort(VAR tab : ARRAY_OF INTEGER)
VAR
   i,j,n,key : INTEGER;
BEGIN
   n := tab.length;
 FOR i FROM 1 TO n STEP 1  DO
      key=tab[i];
      j=i-1;
   END_FOR
 WHILE (j >= 0 && key < tab[j]) DO
        tab[j + 1] = tab[j];
        j=j-1;
   END_WHILE 
      tab[j + 1] = key;
END